import React from 'react';
import PageHeaders from '../../PageHeaders';

const BlogHeader = () => {
    return (
        <PageHeaders
            title="BLOGS"
            description="Explore our latest insights, tips, and updates on education and learning."
        />
    );
};

export default BlogHeader;
